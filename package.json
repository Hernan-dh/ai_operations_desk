{
  "name": "ai-operations-desk",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable operational request triage service",
  "main": "server.js",
  "scripts": { "start": "node server.js", "test": "node --test tests/*.test.cjs" },
  "engines": { "node": ">=20" }
}
